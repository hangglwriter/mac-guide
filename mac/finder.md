# 파인더 & 파일 관리

> 윈도우의 파일 탐색기에 해당하는 맥의 파인더

## 파인더 = 파일 탐색기

윈도우에서 폴더를 열면 "파일 탐색기"가 열리듯, 맥에서는 **Finder(파인더)**가 열린다.

## 윈도우 탐색기와 다른 점

| | 윈도우 탐색기 | 맥 파인더 |
|---|---|---|
| 잘라내기 | Ctrl + X → Ctrl + V | Cmd + C → **Cmd + Option + V** |
| 이름 변경 | F2 | **Enter** |
| 열기/실행 | Enter | **Cmd + 아래** 또는 더블클릭 |
| 경로 입력 | 주소창 클릭 | **Cmd + Shift + G** |
| 숨김 파일 | 폴더 옵션 | **Cmd + Shift + .** |
| 삭제 | Delete키 | **Cmd + Delete** |
| 미리보기 | 없음 | **Space** (Quick Look) |

> **가장 많이 실수하는 것**: 파인더에서 Enter 누르면 열리는 게 아니라 **이름이 바뀐다**!

## 파인더 필수 단축키

| 단축키 | 기능 |
|--------|------|
| Space | **Quick Look** - 파일 미리보기 (PDF, 이미지, 영상 다 됨) |
| Cmd + Shift + G | 경로 직접 입력해서 이동 |
| Cmd + Shift + . | 숨김 파일/폴더 보기/숨기기 |
| Cmd + Option + V | 이동 붙여넣기 (Cmd+C 후 사용) |
| Cmd + Delete | 파일 휴지통으로 보내기 |
| Cmd + 위 | 상위 폴더로 이동 |
| Cmd + 아래 | 폴더 열기 / 파일 실행 |
| Cmd + 1/2/3/4 | 보기 전환 (아이콘/목록/컬럼/갤러리) |

## Quick Look (스페이스바 미리보기)

맥의 숨겨진 보물 기능. 파일 선택 후 **Space**만 누르면 바로 미리보기가 뜬다.

- 이미지, PDF, 영상, 문서 다 지원
- 앱을 열지 않아도 바로 내용 확인
- 여러 파일 선택 후 Space 누르면 슬라이드쇼처럼 넘기기 가능

## 홈 폴더 (~) 구조

```
~/                          ← 홈 폴더 (/Users/storywinner)
├── Desktop/                ← 바탕화면
├── Documents/              ← 문서
├── Downloads/              ← 다운로드
├── Pictures/               ← 사진
├── Music/                  ← 음악
├── Movies/                 ← 동영상
├── Projects/               ← 민티 프로젝트 폴더 (직접 생성)
│   ├── claude-book/        ← 책 프로젝트
│   └── Sites/              ← 웹사이트 프로젝트
└── Applications/           ← 앱 (시스템은 /Applications)
```

## 파인더의 태그 기능

파인더 사이드바에 빨간색, 파란색 같은 색깔 목록이 보이는데, 이게 **태그**다. 폴더와는 다른 파일 분류 시스템.

- **폴더** = 파일을 한 장소에 넣는 것 (물리적 서랍)
- **태그** = 파일에 색깔 스티커를 붙이는 것

하나의 파일에 **여러 태그를 동시에** 붙일 수 있다 (폴더는 한 곳에만 넣을 수 있음). 태그를 클릭하면 여러 폴더에 흩어진 파일을 한 번에 모아볼 수 있다.

### 태그 사용법

| 방법 | 설명 |
|---|---|
| 파일 우클릭 → 태그 선택 | 색깔 태그 붙이기 |
| 사이드바에서 태그 클릭 | 해당 태그가 붙은 파일 모아보기 |
| 파인더 > 설정 > 태그 | 태그 이름 변경, 커스텀 태그 추가 |

> 안 쓰는 사람이 더 많다. 필요 없으면 무시해도 됨!

## 파인더에서 경로 보기

기본적으로 파인더는 현재 어느 폴더에 있는지 경로를 안 보여준다.

**경로 바 켜기**:
- 파인더 메뉴 → 보기 → **경로 막대 보기** (Cmd + Option + P)

그러면 파인더 하단에 현재 경로가 표시된다.

## 폴더에서 터미널 바로 열기

윈도우는 폴더 우클릭하면 "터미널 열기"가 바로 나오지만, 맥은 **기본 비활성화**다. 한 번만 켜두면 윈도우처럼 쓸 수 있다.

### 활성화 방법 (1회만 설정)

1. **시스템 설정** 열기
2. **키보드** → **키보드 단축키…** 클릭
3. 왼쪽 메뉴에서 **서비스** 선택
4. 오른쪽에서 **파일 및 폴더** 펼치기
5. 아래 두 항목 체크 ON
   - **폴더에서 새로운 터미널 열기** (새 창으로 열림)
   - **폴더에서 새로운 터미널 탭 열기** (기존 창의 새 탭으로 열림)

### 사용법

Finder에서 폴더 위에서 **우클릭** → 메뉴 하단 **"서비스"** → 원하는 옵션 선택

### 새 창 vs 새 탭

| | 동작 | 언제 쓰면 좋나 |
|---|---|---|
| **새 창** | 별도 터미널 창으로 뜸 | 다른 작업과 분리해서 보고 싶을 때 |
| **새 탭** | 기존 터미널 창에 새 탭 추가 | 이미 터미널 켜져 있을 때 빠르게 이동 |

> **꿀팁**: 자주 작업하는 폴더(예: 프로젝트 폴더)를 **사이드바에 즐겨찾기**로 등록해두면, 우클릭 한 번으로 클로드 코드 작업 시작 가능!

### 더 빠른 대안

| 방법 | 설명 |
|---|---|
| **드래그 앤 드롭** | 터미널에 `cd ` 입력 후 폴더를 터미널 창으로 드래그 → 경로 자동 입력 |
| **iTerm2 사용** | iTerm2 설치 시 동일 메뉴에 "New iTerm2 Tab/Window Here" 자동 추가 |
